<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.panupong.exercise">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Struct_Func.class_func"/>
        <activity android:name=".Array_Func.Array_mod"/>
        <activity android:name=".bigO"/>
        <activity android:name=".BinarySearchTree"/>
        <activity android:name=".observ.ObserverPattern"/>
        <activity android:name=".Mathematics"
            android:theme="@style/AppTheme"/>
    </application>

</manifest>